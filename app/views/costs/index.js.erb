$(document).ready(function(){
  <% if @place %>
    $('#place_<%= @place.id %>_other_costs').html('<%= j render 'section',plus: {things: @costs.where('costs.id != ?', params[:id]),title:  "#{@place}的其他消费记录",section_path: place_costs_path(@place)} %>');
  <% end %>
});
