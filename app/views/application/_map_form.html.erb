<% content_for :head do -%>
  <script src="http://maps.google.com/maps/api/js?sensor=false&libraries=places" type="text/javascript"></script>
  <script>
    lat = <%= cookies[:lat].blank? ? (resource.try(:lat) || 39.9042) : (resource.try(:lat) || cookies[:lat] || 39.9042) %>;
    lon = <%= cookies[:lon].blank? ? (resource.try(:lon) || 116.407) : (resource.try(:lon) || cookies[:lon] || 116.407) %>;
    google.maps.event.addDomListener(window, 'load',form_map_init );
  </script>
<% end -%>
<% content_for :sidebar do -%>
  <section>
    <header>
      <nav><h2 id="地图选址"><a href='#地图选址'>地图选址</a></h2></nav>
    </header>
    <div class="article">
      <div id="google_map" style="height:400px;">
      </div>
    </div>
  </section>
<% end -%>
