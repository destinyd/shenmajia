var obj_locate = $('#locate');
if(obj_locate.length <= 0)
  $('body').append('<%=j(render 'locates/form') %>');
var obj_locate = $('#locate');
obj_locate.dialog();
$('input#id').autocomplete({
  source: '/cities/search.json',
  minLength: 1,
  select: function(e,ui){
    $('#input#id').val(ui['item']['id']);
  },
});