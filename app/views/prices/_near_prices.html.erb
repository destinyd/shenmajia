<%= section :title => '附近特价商品信息' do %>
  <ul class="nowrap">
    <% near_prices.each do |price| %>
      <li>
      <%=link_to price.human_name,price, :title => price.human_name %><%= price.address %>
      </li>
    <% end %>
  </ul>
<% end unless near_prices.blank? %>
