page = <%= params[:page] || 1 %>;
<% collection= Place.near([city_info_of_ip[:lat],city_info_of_ip[:lon]]).paginate(page: params[:page]) %>
poses = [
<% @places.each do |resource| %>
{
  pos: new google.maps.LatLng(<%= resource.lat %>, <%= resource.lon %>),
    title:'<%= resource.name %>',
    id:'<%= resource.id %>'
}
<%= ',' unless @places.last == resource %>
<% end -%>
];
put_markers();
<% if params[:swlat].nil? %>
  fit_bounds()
<% end %>
