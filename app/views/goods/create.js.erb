<% if @good.valid? %>
  add_good('<%= @good.id %>','<%= j @good.name %>');
  $('#modalNewGood').modal('hide')
  $('#new_good').get(0).reset()
<% else %>  
  $('#modal_new_good').html('<%= j(render 'form',good: @good) %>');
<% end %>
