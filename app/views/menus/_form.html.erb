<%= simple_form_for([@place,@menu]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tag_list,placeholder: '奶茶类' %>
    <%= f.input :image %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
