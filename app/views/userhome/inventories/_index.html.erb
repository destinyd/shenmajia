<ul id="inventories">
<% inventories.each do |inventory| %>
  <li>
    <div>
      <div class="pic">
        <%= link_to inventory.good do  %>
        <%= img_upload inventory.good.uploads.first,inventory.good,:shop %>
        <% end -%>
      </div>
      <div class="detail">
        <p class="title"><%= link_to inventory.good,inventory.good,title: inventory.good %></p>
        <p><b>单价</b>:<%= inventory.current %><%= "(#{'%.1f' % (inventory.current / inventory.original*10)}折)" if inventory.original and inventory.original > 0 %></p>
        <%= p_field inventory,:original %>
        <%= p_field inventory,:amount %>
      </div>
      <div class="tc">
        <%= button_to '修改', edit_userhome_inventory_path(inventory), method: :get %>
        <%= button_to '删除', userhome_inventory_path(inventory), method: :delete, confirm: '你确定？' %>
      </div>
    </div>
  </li>
<% end -%>
</ul>
<div class='c'></div>
